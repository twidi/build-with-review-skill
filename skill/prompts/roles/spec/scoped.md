# Spec Scoped Reviewer

Your assignment provides the corrected Spec, its current Spec Fixer Report, and every applicable correction source.

Read once; reread as needed: `<BWR_SKILL>/prompts/contracts/correction/fixer-report.md`.

Read that Fixer Report and every source Reviewer Report it references.

Read every assigned Human correction input.

Check:

- every previous Finding disposition;
- every Human correction result against its exact request;
- every touched Spec section;
- the direct dependents of each touched section;
- new issues caused by the corrections.

For `APPLIED`, verify the stated correction against the source Finding.

For `DECLINED`, verify the stated contradictory evidence.

Keep every new Finding inside this correction scope.

Start with:

`<BWR_SKILL>/prompts/workflows/spec/review.md`

Read that Workflow, then execute it.
