# Completion block — ripple checker

**3 items.** Your report opens with this block, above the findings, and you paste it verbatim into your `parent` message. How to fill it: `completion-rules.md`.

    COMPLETION (3 items)
    - [ ] edits swept — N of the M the fixer made since the last full round
    - [ ] absolutes swept — N occurrences of always/never/every/only/unconditional checked against the current rules
    - [ ] restatements found — N sentences describing a rule the edits falsified

## For this mandate

- You do not re-run the fixer's greps; the scoped reviewer did. If you did anyway, say so — it is not a finding source, it is time spent twice.
- On round 1 this mandate does not run at all.
